// Declaration --------------------------------------

module.exports = {
  add,
  subtract,
  // ...
};

// Implementation ----------------------------------

function add(x, y) {
  return x + y;
}

function subtract(x, y) {
  return x - y;
}
